#pragma once
#include <glad/glad.h>
#include <iostream>
#include <vector>
#include <random>
#include <glm/glm.hpp>

// class GTAOBuffer
// {
// public:
//     GTAOBuffer(int size_x, int size_y) { aobuffer_init(size_x, size_y); }
//     ~GTAOBuffer() { cleanUp(); }
//     void bindToWrite();
//     void bindToRead();
//     void bindTextureByIndex(int index);

// private:
//     void aobuffer_init(int size_x, int size_y);
//     void cleanUp();
//     GLuint aobuffer;
//     GLuint gtao_color_text;
// };
